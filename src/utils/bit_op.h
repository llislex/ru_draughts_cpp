#pragma once

unsigned lsb(unsigned bmp);
unsigned msb(unsigned bmp);
//unsigned bit_num_dir(unsigned bmp, bool msb = true);
unsigned take_msb(unsigned& bmp);
unsigned take_lsb(unsigned& bmp);
unsigned bitcount(unsigned bmp);

